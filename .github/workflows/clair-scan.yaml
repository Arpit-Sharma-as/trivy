name: Minimal Clair Scan

on:
  workflow_dispatch:
    inputs:
      image:
        description: "Docker image to scan (e.g. nginx:1.25)"
        required: true
        default: "nginx:latest"

jobs:
  clair-scan:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Go & install clairctl
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - run: |
          go install github.com/quay/clair/v4/cmd/clairctl@latest
          echo "$HOME/go/bin" >> $GITHUB_PATH

      - name: Start Clair
        run: |
          git clone https://github.com/quay/clair.git
          cd clair
          docker compose -f docker-compose.yaml up -d

      - name: Wait for Clair API
        run: |
          for i in {1..30}; do
            if curl -s http://localhost:6060/ > /dev/null; then
              echo "✅ Clair API is ready!"
              exit 0
            fi
            echo "⏳ Waiting..."
            sleep 10
          done
          exit 1

      - name: Scan Image
        run: |
          clairctl report \
            --host http://localhost:6060 \
            -o json ${{ github.event.inputs.image }} \
            > clair-report.json

      - name: Upload report
        uses: actions/upload-artifact@v4
        with:
          name: clair-report
          path: clair-report.json